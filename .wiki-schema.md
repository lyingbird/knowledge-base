# Wiki Schema（知识库配置规范）

> 这个文件告诉 AI 如何维护这个知识库。你和 AI 可以一起调整它。

## 知识库信息

- 主题：AI 时代的组织、产品、游戏、治理与人类独特性
- 创建日期：2026-06-03
- 语言：中文
- 版本：1.1

## 目录结构

```text
knowledge-base/
├── raw/                    # 原始素材或待消化材料
│   ├── articles/           # 网页文章
│   ├── tweets/             # X/Twitter 内容
│   ├── wechat/             # 微信公众号文章
│   ├── xiaohongshu/        # 小红书内容
│   ├── zhihu/              # 知乎内容
│   ├── pdfs/               # PDF 文件
│   ├── notes/              # 手写笔记、转写、外部 Markdown
│   │   └── video-notes/    # 从 02-视频笔记/ 复制来的不可变素材副本
│   └── assets/             # 图片等附件
├── wiki/                   # 持续维护的知识库主体
│   ├── entities/           # 实体页：人物、组织、概念、工具
│   ├── topics/             # 主题页：研究主题、知识领域
│   ├── sources/            # 素材摘要页：每份素材一页
│   ├── comparisons/        # 对比分析页
│   ├── synthesis/          # 综合分析页
│   └── queries/            # 保存的查询结果
├── 00-入口/                # 人类阅读入口
├── 01-主题/                # 历史主题页
├── 02-视频笔记/            # 历史视频学习笔记
├── 03-核验/                # 待核验事实
├── index.md                # 新知识库索引
├── log.md                  # 操作日志
├── purpose.md              # 研究目的与方向
└── .wiki-schema.md         # 本文件
```

## Karpathy LLM Wiki 三层约定

本库按 Karpathy `LLM Wiki` 方法维护，核心不是 RAG 检索，而是把知识“编译一次并持续维护”。

### 1. Raw sources：原始素材层

- 位置：`raw/`，当前历史视频笔记已复制到 `raw/notes/video-notes/`。
- 角色：事实来源和可回溯材料。
- 规则：LLM 可以读取，但默认不修改；需要纠错时，优先在 `03-核验/待核验事实.md` 和对应 `wiki/sources/` 里记录，而不是改写原始素材。

### 2. Wiki：LLM 维护层

- 位置：`wiki/`。
- 角色：由 LLM 生成和维护的结构化知识层，包括素材摘要、实体页、主题页、对比、综合和图谱数据。
- 规则：新增素材时，不只是摘要；必须把新信息整合进已有实体页和主题页，补交叉引用，标记矛盾或待核验点。

### 3. Schema：维护规范层

- 位置：`.wiki-schema.md` 和 `AGENTS.md`。
- 角色：告诉 LLM 如何维护本库。
- 规则：随着使用经验持续演化；当发现新的命名、引用、核验或图谱规则，应更新 schema。

## 页面命名规范

- 实体页：`wiki/entities/{名称}.md`
- 主题页：`wiki/topics/{主题名}.md`
- 素材摘要：`wiki/sources/{日期}-{短标题}.md`
- 对比分析：`wiki/comparisons/{对比主题}.md`
- 综合分析：`wiki/synthesis/{分析主题}.md`

## 页面格式规范

每个 wiki 页面应包含：

```markdown
---
tags: [标签1, 标签2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: [关联素材列表]
---

# 页面标题

> 一句话摘要

## 核心内容

...

## 相关页面

- 另一个页面
```

## 交叉引用规范

- 页面间使用 Obsidian 双向链接语法，例如 `［［真实页面名］］`（实际写页面时使用半角双方括号）。
- 链接到旧笔记时保留路径，例如 `[[02-视频笔记/Tom Blomfield：公司不再是组织，而是 AI 系统]]`。
- 新的 wiki 层优先使用短页面名，例如 `[[AI原生公司]]`、`[[智能密度]]`。
- 每个实体页和主题页底部维护“相关页面”。

## Ingest（消化素材）规则

完整处理：

1. 保存或定位原始素材。
2. 在 `wiki/sources/` 创建素材摘要页。
3. 提取 3-5 个关键概念。
4. 创建或更新 `wiki/entities/`。
5. 创建或更新 `wiki/topics/`。
6. 检查新信息是否矛盾、修正或强化旧页面中的判断。
7. 更新 `index.md` 和 `log.md`。
8. 如影响知识库全貌，更新 `wiki/overview.md`。
9. 需要时重新生成 `wiki/graph-data.json` 和 `wiki/knowledge-graph.html`。

简化处理：

1. 创建素材摘要页。
2. 提取 1-3 个关键概念。
3. 已存在实体页则补充信息；不存在则在摘要页标记“待创建”。
4. 更新 `index.md` 和 `log.md`。

## 别名词表

```text
LLM = 大语言模型 = 大模型 = Large Language Model
AI native company = AI 原生公司 = AI原生组织 = AI 原生组织
agent = 智能体 = AI agent
enterprise context = 企业上下文 = 商业上下文
intelligence density = 智能密度
business loop = 业务闭环 = 闭环
AI game = 游戏 AI = AI 游戏
AI safety = AI 安全 = AI 治理
```

## Query（查询）规则

1. 先读 `index.md`，定位相关条目。
2. 再在 `wiki/` 下搜索关键词和别名。
3. 阅读相关页面后综合回答。
4. 回答中标注来源页面。
5. 如果回答形成了可复用结论，保存到 `wiki/synthesis/` 或 `wiki/comparisons/`，并更新 `index.md` 与 `log.md`。

## Lint（健康检查）规则

检查项：

- 孤立页面：没有入链或出链。
- 断链：双向链接指向不存在页面。
- 索引不一致：`index.md` 未覆盖重要页面。
- 概念重复：同一概念存在多个近义页。
- 待核验事实：高风险事实是否进入 `03-核验/待核验事实.md`。
- 停滞综合：新素材是否没有反映到已有实体页、主题页或综合页中。
- 素材孤岛：`wiki/sources/` 是否只链接原文而没有链接到实体或主题。

## 关系类型词汇表

| 类型关键词 | 含义 | Mermaid 写法示例 |
|---|---|---|
| 实现 | A 是 B 的具体实现 | `A -->|实现| B` |
| 依赖 | A 依赖 B 才能工作 | `A -->|依赖| B` |
| 对比 | A 与 B 是同类可比较对象 | `A -->|对比| B` |
| 矛盾 | A 与 B 存在观点冲突 | `A -->|矛盾| B` |
| 衍生 | A 从 B 演化而来 | `A -->|衍生| B` |
